export interface ServiceDate {
  id: string;
  title: string;
  price: number;
  duration: Date;
}

export interface ServiceMin {
  id: string;
  title: string;
  price: number;
  durationMin: number;
}